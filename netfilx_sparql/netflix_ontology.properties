#Fri May 15 22:45:57 CEST 2026
# LEGACY / UNUSED by the docker-compose stack (docker-compose.yml at repo root) -- that stack
# configures Ontop via ONTOP_DB_URL/ONTOP_DB_USER/ONTOP_DB_PASSWORD environment variables
# sourced from the repo-root .env file instead, so no password needs to live in this file.
# If you use this .properties file directly with a standalone Ontop CLI, fill in your own
# local password below -- do not commit a real password here.
jdbc.password=CHANGEME
jdbc.user=postgres
jdbc.url=jdbc\:postgresql\://localhost\:5432/netflix_vkg
jdbc.driver=org.postgresql.Driver
